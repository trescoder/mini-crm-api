// import { NewClient } from "../interfaces/new-client";

// export function extractClientInfo(data: NewClient) {
//   return Object.freeze(
//     ({ name, last_name, email, birthday, tel, address, contact_type, origin } =
//       data)
//   );
// }
